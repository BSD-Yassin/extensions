# Jira Extension Workspace Notes

This directory is reserved for local AI/dev workflow files while iterating on the `jira` Vicinae extension.

Suggested usage:
- Put prompt/context snippets here for feature parity tasks.
- Store small debug checklists for manual extension testing.
- Keep this folder lightweight and extension-scoped.

Current debug log file for the extension (when `Debug Mode` is enabled):
- `/tmp/vicinae-jira-extension.log`
